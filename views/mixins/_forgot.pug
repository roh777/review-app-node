mixin forgotForm()
    form.form(action="/forgot" method="POST")
        h2 I Forgot Password
        label(for="email") Email
        input(type="email" name="email")
        input.button(type="submit" value="Send a Reset")